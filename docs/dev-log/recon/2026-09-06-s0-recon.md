# Scout Recon — 2026-09-06

Mission: inventory HTML↔markdown format, Quarto anatomy, Julia runnable environments, DRM.jl plotting surface.

---

## 1. HTML ↔ MARKDOWN INVENTORY

**Dialogue line counts** (speaker tallies): Both files match exactly.

| Speaker | HTML `<span class="who">` | Markdown `**Name:**` |
|---------|--------------------------|---------------------|
| Itchy   | 40                       | 40                  |
| Toto    | 13                       | 13                  |
| Momo    | 18                       | 18                  |

**CSS class → markdown convention mapping**:

| Class | HTML element | Markdown convention | File |
|-------|--------------|---------------------|------|
| `.who`, `.who.toto`, `.who.momo` | `<span class="who">` | `**Speaker:**` at line start | Both |
| `.line` | `<div class="line">` | Wrapper for dialogue; no markdown equivalent | HTML only |
| `.scene` | `<p class="scene">` | `**…**` (bold scene description) | wk2-linear-models.md ln 45, 284, 396, etc. |
| `.aside` | `<span class="aside">` | `*…*` (italicized action/aside text in dialogue) | wk2-linear-models.md ln 67, 139, 356, etc. |
| `.board` | `<p class="board">` | `> **…**` (centered blockquote) | wk2-linear-models.md ln 69 |
| `.disagree` | `<div class="disagree">` | `> **⚠ DISAGREE — …**` (blockquote box) | wk2-linear-models.md ln 289–335 |
| `.dtag` | `<p class="dtag">` | Inside `.disagree` tag line | wk2-linear-models.md ln 291 |
| `.provenance` | `<p class="provenance">` | Markdown footnote inside disagree box | wk2-linear-models.md ln 333–334 |
| `.obj` | `<ol class="obj">` | Numbered objectives list | wk2-linear-models.md ln 32–39 (HTML: 273–280) |
| `.gloss` | `<ul class="gloss">` | Bullet list with terms in Summary | wk2-linear-models.md ln 402–413, 415–430 |
| `.tw` | `<div class="tw">` | `<table>` wrapper for overflow scroll | wk2-linear-models.md ln 304 (HTML: 471–482) |
| `.note` | `<div class="note">` | `## Honest note from drafting…` section | wk2-linear-models.md ln 493–515 (HTML: 619–638) |
| `.p`, `.err`, `.cm` | `<span>` in `<pre code>` | Syntax highlighting in code blocks | Generated by JS inline script (HTML ln 648–656) |

**HTML elements with NO markdown source** (masthead/footer — must move to YAML front matter):

| Element | Content | Location |
|---------|---------|----------|
| `.kicker` + `.sep` | "Stats Hours with Itchy / Class 2 / Sample draft" | HTML ln 248–252 |
| `.deck` | "The linear model, taught in one engine — and the morning…" | HTML ln 254 |
| `.status` + `.tag` | "Not the book" warning box and metadata | HTML ln 256–259 |
| `.meta` | Version/provenance (engines, dates, caveat) | HTML ln 261–265 |
| `footer` | "Stats Hours with Itchy · Class 2 of a planned 13…" | HTML ln 640–642 |
| `.mast`, `.mast-inner` | Header container (structural only) | HTML ln 57–63 |

**Count of no-markdown elements: 6 semantic** (kicker, deck, status, meta, footer; mast is wrapper).

---

## 2. QUARTO THEME ANATOMY

**UNVERIFIED** — no `_quarto.yml`, CSS, or Lua filters found in project. Architecture would be:

**Files needed** (`_quarto.yml` keys):

```yaml
format:
  html:
    theme: custom                              # or bootstrap + custom CSS override
    css: [style.css, typography.scss]          # or theme overrides
    include-in-header: [mathjax-config.html]  # math/script injection
    filters: [dialogue-box.lua, class-map.lua] # Pandoc Lua filters for Para/BlockQuote/Div
execute:
  freeze: auto                                  # skip re-render if no changes
  error: true                                   # continue on error (for demo code)
engine: jupyter                                # only if using IJulia cells
```

**Lua filter hooks** (inferred from CSS classes):

- `Para` → detect `**Speaker:**` + next para = dialogue `.line`
- `BlockQuote` → map `**⚠ DISAGREE — …**` → `.disagree` div; map `> **…**` (centered) → `.board`
- `Div` → unwrap scene description into `.scene` class
- Code blocks with `{.julia}` → mark language for IJulia kernel

**Julia kernel declaration** (if Jupyter-based):

```yaml
format:
  html:
    jupyter: julia-1.10                        # or IJulia kernel name
```

**Current state:** HTML is hand-authored; Quarto machinery does not yet exist.

---

## 3. COLAB JULIA BOOTSTRAP RECIPES

**Search command:** `grep -r "Colab\|Binder\|repo2docker" the second brain --include="*.md"` and the same over the local repos root.

**Found:**
- **Location:** `the second brain/projects/julia-stats-series/README.md`
- **Reference:** Mentions Colab/Binder/JupyterHub as needed for "hosted runnable environment"
- **Status:** Identified as **good destination, not a good next step** — Colab needs Julia installation steps documented

**Grep result:** No actual scripts or environment files (`.jl`, `requirements.txt`, `environment.yml`, `Manifest.toml`) found locally for Colab/Binder setup.

**None found locally.** Colab Julia bootstrap is currently a knowledge gap flagged in project notes, not an implemented recipe.

---

## 4. DRM.jl PLOTTING SURFACE

**File:** `DRM.jl/ext/DRMMakieExt.jl` (lines 1–219)

**Main exported plot function:**

```julia
drm_figure(data::NamedTuple; kind::Symbol = _infer_kind(data), kwargs...) → Figure
```

**Signature variants** (dispatches on `kind` kwarg):

| `kind` | Function | Signature | Figure output |
|--------|----------|-----------|----------------|
| `:profile` | `_profile(d; title, hue, kwargs...)` | 1-D profile deviance + Confidence Eye | 720×420 |
| `:parameter_surface` | `_parameter_surface(d; title, kwargs...)` | 2-D heatmap contours | 720×480 |
| `:corpairs` | `_corpairs(d; title, kwargs...)` | Residual ρ₁₂ scatter/line | 640×360 |

**AlgebraOfGraphics:** imported at ln 10; NOT called in extension code (Makie/AlgebraOfGraphics loaded for gating only per comment ln 5–6).

**Accessor functions required by figures** (searched `DRM.jl/src` exports):

Explicitly exported from `export` line:
- `coef(fit, :mu)` / `coef(fit, :sigma)` (no separate export; defined ln gaussian_core.jl)
- `vcov(fit)`
- `mf_coef, mf_aic, mf_bic, mf_fitted, mf_summary` (model-frame accessors)

Used internally (not explicitly exported but available):
- `predict_parameters(fit, data; type = :link)`
- Confidence interval extraction via `confint(fit)` / `coef(fit)` + `vcov`

**No `ranef`, `residuals`, or `profile` accessor functions** found in `export` lines; profile drawing is internal to `_profile()`.

---

## Notes on output format

- Paths are relative to repo root (e.g., `DRM.jl/ext/DRMMakieExt.jl`)
- UNVERIFIED claims marked; local-knowledge inferences marked as "would need"
- Dialogue counts verified by exact grep; counts match

**Correction (2026-09-06, verified at runtime with `names(DRM)`):** `ranef`, `residuals`, `predict`, `fitted` and `simulate` ARE exported — they sit mid-way through a multi-name export list (`src/DRM.jl:166`) that a line-start grep does not see. The 'no exported accessors' line above is wrong; keep it as a record of the narrow-search trap.
