# Binder configuration

`Project.toml` + `Manifest.toml` pin DRM.jl by GitHub URL (no registry needed) and the plotting
stack; `postBuild` precompiles at image build. Launch (once the repository is public):

https://mybinder.org/v2/gh/itchyshin/stats-hours/main?labpath=notebooks%2Fwk2-linear-models.ipynb
